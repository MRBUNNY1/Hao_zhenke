#include <iostream>

using namespace std;

int main()
{
    double a,b,c;


    while(a*b*c>0)
    {

    cout<<"input a,b,c"<<endl;
     cin>>a,b,c;
    if(a+b>c,b+c>a,a+c>b)
    cout<<"可以组成三角形"<<endl;
    else
    cout<<"不可以组成三角形"<<endl;
    }
}
